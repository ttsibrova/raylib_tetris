#include <graphics/sprites.h>
#include <graphics/decorative_block.h>

std::unique_ptr<IDrawableObject> SpriteGraphicGenerator::GetLetterT (const Color& mainColor, const Color& shadeColor)
{
    auto block = std::unique_ptr <DecorativeBlock> ();
    return block;
}
